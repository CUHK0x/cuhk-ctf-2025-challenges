# Build
To create the challenge release archive:
```bash
npm install # Install both production and dev dependencies
npm run generate-data # Generate the data to be loaded in the challenge
npm run build
npm run pkg-challenge # Package the program
```

# Development Server
```bash
npm run dev
```